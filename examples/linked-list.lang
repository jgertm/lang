(defmodule examples.linked-list)

(deftype IntList
  (| [:nil Unit]
     [:cons {Integer IntList}]))

(defn empty? [list]
  (match list
    ([:nil _] true)
    ([:cons _] false)))

; (defn sum [list]
  ; (match list
    ; ([:nil _] 0)
    ; ([:cons {n tl}] (+ n (sum tl)))))

(defn main []
  (empty? [:cons {2 [:nil nil]}]))
